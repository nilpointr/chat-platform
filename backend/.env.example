ANTHROPIC_API_KEY=

# Set to true to skip Anthropic API calls and return canned responses.
# Useful for frontend development and integration testing without burning credits.
MOCK_MODE=false

# Origin allowed to call this API via CORS.
FRONTEND_ORIGIN=http://localhost:3000
